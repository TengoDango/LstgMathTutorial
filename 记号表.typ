#import "template": *

#math-tutorial[
  #let s0 = []
  #let s1-1 = [1.1 极坐标系与三角函数]
  #set align(center)
  #table(
    columns: (auto, auto, auto, auto),
    table.header([名称], [记号], [说明], [对应章节]),
  )
]
